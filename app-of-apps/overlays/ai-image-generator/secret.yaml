apiVersion: v1
kind: Secret
metadata:
  name: ai-image-generator-secrets
  namespace: ai-lounge
type: Opaque
stringData:
  # OpenAI API Key
  # 실제 배포 시에는 이 값을 외부 시크릿 관리 시스템에서 주입받도록 설정
  OPENAI_API_KEY: ""
