apiVersion: v1
kind: Secret
metadata:
  name: chat-history-viewer-secrets
  namespace: ai-lounge
type: Opaque
stringData:
  # PostgreSQL 연결 정보
  # 실제 배포 시에는 postgres 시크릿에서 주입받도록 설정
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "ai_lounge"
  DB_USER: "postgres"
  DB_PASSWORD: "postgres"
