apiVersion: v1
kind: Service
metadata:
  name: chat-history-viewer
  namespace: ai-lounge
  labels:
    app: chat-history-viewer
spec:
  type: ClusterIP
  selector:
    app: chat-history-viewer
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http
